---
layout: "main.liquid"
pagination:
  data: collections
  size: 1
  alias: slug
  addAllPagesToCollections: true
  filter:
    - all
    - post
    - posts
    - tagList
eleventyComputed:
  page_title: "{{ slug | categoryOrTagName: collections[slug][0] }}"
  og_description: "Elle Mundy's {{ slug | categoryOrTagName: collections[slug][0] | downcase }}"
permalink: "/{{ slug }}/"
---
{% assign articles = collections[ slug ] %}
{% assign collection = slug | categoryOrTag: articles[0] %}
{% assign isTagPage = slug | isTag %}

{% render 'collection',
  collection: collection,
  articles: articles,
  isTagPage: isTagPage,
  class: "single"
%}
