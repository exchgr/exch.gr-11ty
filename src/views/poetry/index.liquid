---
layout: "main.liquid"
pagination:
  data: categories.poetry.attributes.articles.data
  size: 10
eleventyComputed:
  page_title: "{{ categories.poetry.attributes.name }}"
---
{% render 'collection',
  collection: categories.poetry,
  articles: pagination.items,
  pagination: pagination,
  count: pagination.items.size,
  class: "single"
%}
