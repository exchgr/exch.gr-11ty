---
layout: "main.liquid"
pagination:
  data: categories.code.attributes.articles.data
  size: 10
eleventyComputed:
  page_title: "{{ categories.code.attributes.name }}"
---
{% render 'collection',
  collection: categories.code,
  articles: pagination.items,
  pagination: pagination,
  count: pagination.items.size,
  class: "single"
%}
